"use strict";


/* ===== Stickyfill ===== */
/* Ref: https://github.com/wilddeer/stickyfill */
// Add browser support to position: sticky

var elements = document.querySelectorAll('.sticky');
Stickyfill.add(elements);

/* ===== Gumshoe SrollSpy ===== */
/* Ref: https://github.com/cferdinandi/gumshoe  */
// Initialize Gumshoe
var spy = new Gumshoe('#doc-nav a', {
	offset: 0 
});




/* ====== SimpleLightbox Plugin ======= */
/*  Ref: https://github.com/andreknieriem/simplelightbox */
var lightbox = new SimpleLightbox('.simplelightbox-gallery-1 a', { /* options */ });
var lightbox = new SimpleLightbox('.simplelightbox-gallery-2 a', {/* options */});
var lightbox = new SimpleLightbox('.simplelightbox-gallery-3 a', {/* options */});
var lightbox = new SimpleLightbox('.simplelightbox-gallery-4 a', {/* options */});
var lightbox = new SimpleLightbox('.simplelightbox-gallery-5 a', {/* options */});
var lightbox = new SimpleLightbox('.simplelightbox-gallery-6 a', {/* options */});
var lightbox = new SimpleLightbox('.simplelightbox-gallery-7 a', {/* options */});
var lightbox = new SimpleLightbox('.simplelightbox-gallery-8 a', {/* options */});
var lightbox = new SimpleLightbox('.simplelightbox-gallery-9 a', {/* options */});
var lightbox = new SimpleLightbox('.simplelightbox-gallery-10 a', {/* options */});
var lightbox = new SimpleLightbox('.simplelightbox-gallery-11 a', {/* options */});
var lightbox = new SimpleLightbox('.simplelightbox-gallery-12 a', {/* options */});
var lightbox = new SimpleLightbox('.simplelightbox-gallery-13 a', {/* options */});
var lightbox = new SimpleLightbox('.simplelightbox-gallery-14 a', {/* options */});
var lightbox = new SimpleLightbox('.simplelightbox-gallery-15 a', {/* options */});
var lightbox = new SimpleLightbox('.simplelightbox-gallery-16 a', {/* options */});
var lightbox = new SimpleLightbox('.simplelightbox-gallery-17 a', {/* options */});
var lightbox = new SimpleLightbox('.simplelightbox-gallery-18 a', {/* options */});
var lightbox = new SimpleLightbox('.simplelightbox-gallery-19 a', {/* options */});
var lightbox = new SimpleLightbox('.simplelightbox-gallery-20 a', {/* options */});
var lightbox = new SimpleLightbox('.simplelightbox-gallery-21 a', {/* options */});
var lightbox = new SimpleLightbox('.simplelightbox-gallery-22 a', {/* options */});
var lightbox = new SimpleLightbox('.simplelightbox-gallery-23 a', {/* options */});
var lightbox = new SimpleLightbox('.simplelightbox-gallery-24 a', {/* options */});



